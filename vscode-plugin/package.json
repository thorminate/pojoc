{
  "name": "pojoc-vscode",
  "displayName": "Pojoc",
  "description": "Language support for Pojoc schema files",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "pojoc",
        "aliases": [
          "Pojoc"
        ],
        "extensions": [
          ".pojoc"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "pojoc",
        "scopeName": "source.pojoc",
        "path": "./syntaxes/pojoc.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Pojoc",
      "properties": {
        "pojoc.serverPath": {
          "type": "string",
          "default": "pojoc-lsp",
          "description": "Path to the pojoc-lsp executable"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
